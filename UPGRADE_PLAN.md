# Upgrade Plan — Auto Video Editor / AI Video Automation

Live, per-phase checklist for the upgrade spec in `Prompt nâng cấp Auto Video Editor tích hợp CapCut + AI.md` (34 numbered sections). Tracked separately from `IMPLEMENTATION_PLAN.md`, which covers the *original* master-prompt phases (0–13) and is now 100% complete — do not merge these two files or re-litigate anything already decided there.

## Audit summary (done before any code, per the upgrade spec's own §30 instruction)

Full audit given to the user directly in conversation on 2026-09-06 — not duplicated here verbatim, summarized:

- **CapCut integration**: real, already built (Phase 9 of the original plan) — but architecturally **direct draft-file writing** (`Project → CapCutExportGraph → CapCutAdapter → real draft JSON on disk`), **not GUI/RPA automation**. The upgrade spec's own §12/§15/§16 (CapCut Automation Layer, Worker/Resource Management, Failure Recovery from "CapCut crash"/"CapCut không mở") all assume a GUI-automation model. **User's explicit decision (2026-09-06): keep the direct draft-file approach, do NOT build RPA/GUI automation.** This makes §15's CapCut Worker Pool and most of §16's CapCut-process-crash recovery scenarios inapplicable by construction — there is no live CapCut process to crash or lose track of. Any future decision to revisit RPA is a separate, explicit product decision (same open item already tracked in `IMPLEMENTATION_PLAN.md`'s Phase 9 section) — not something this upgrade silently reopens.
- **AI integration**: real, already built (Phase 10) — provider-agnostic (`ai::provider`), `EditPlan` closed-schema pipeline, Smart Edit, NL command box, AI-assisted highlight detection, B-roll suggestion, media tagging. No streaming, no automatic retry, no token/cost tracking yet.
- **Job/queue/worker**: real, already built (Phase 11) — `batch::manager`/`batch::pipeline`, one worker thread per batch, sequential, real pause/resume/cancel/retry.
- **Templates**: real, already built (Phase 11) — `templates::mod::Template`, 8 built-ins + custom, save/import/export. No versioning, no intro/outro/watermark/background-music fields, no asset-by-id references yet.
- **Database**: `db::` (SQLite media index, separate from `project.json`), `templates::io` (JSON-file-per-template). No history/job-log table yet.

**User's confirmed direction (2026-09-06)**: implement phase-by-phase per the upgrade spec's own suggested breakdown, verifying and reporting back after each phase before starting the next. The phases below are *adapted* from the spec's own §31 breakdown to reflect what's already real vs. genuinely new — not a verbatim copy, since large parts of the spec's own "Phase 1/2" already exist.

---

## Phase U1 — Asset Library, Template enhancements, Multi-template batch

- [x] Asset Library (§17): a real catalog for intro/outro/logo/watermark/music/sound-effect/overlay/font/subtitle-style/transition-preset/background, each referenced by a stable id (not a hardcoded path). CRUD (add/list/remove), local-file-backed (mirrors `templates::io`'s existing storage-location convention).
  - New `crate::assets` module (`src-tauri/src/assets/{mod,io,error}.rs`): `AssetKind` (closed enum, §17's exact 11-kind list), `Asset { id, kind, name, file_path, created_at, tags }`, `new_asset(kind, name, file_path)` (real existing-file check via `Path::is_file()`, rejects a nonexistent/non-file path before ever registering it). Storage: `$APPLOCALDATA/assets/<id>.json`, atomic temp-file-then-rename writes — deliberately the same simpler JSON-file-per-item convention `templates::io` already uses (not a `db::MediaLibrary`-style SQLite table): this catalog is a small, curated, user-populated list (a handful of "the logo"/"the outro"/"my music track" entries), not a bulk-import/searchable media index, so SQLite's query/pagination machinery would be unused weight. Files are referenced by absolute path, never copied into app-data (same convention `project::MediaItem::source_path` already uses for external media).
  - Tauri commands (`commands/assets.rs`, registered in `lib.rs`): `list_assets(kind: Option<AssetKind>)`, `add_asset(kind, name, file_path)`, `remove_asset(asset_id)`, `update_asset(asset_id, name?, file_path?, tags?)`.
  - **Which kinds are really consumed vs. structural-only** (documented in `assets::mod`'s module doc comment): **real** — `Intro`/`Outro` (referenced from `Template::intro`/`outro`, resolvable to a real video file); `Logo`/`Watermark` (an image file, referenced from `Template::watermark`); `Music` (an audio file, referenced from `Template::background_music`, could feed `AudioClipSettings`/`AudioRole::Music` once a caller builds a project from a template). **Structural-only** (§17 lists them, no consuming feature exists yet anywhere in this codebase — same honest treatment `templates::mod`'s own `TransitionSettings`/`SportsOverlaySettings` already use): `SoundEffect`, `Overlay`, `Font`, `SubtitleStyle`, `TransitionPreset`, `Background`. `SubtitleStyle` was deliberately kept as a bare `file_path` reference rather than special-cased to reference an existing `CaptionStyle` id — `Template::caption_style` is already a whole owned, fully-real `CaptionStyle`, so there's no real consumer this kind would plug into today.
  - Catalog starts empty — no built-in seed assets, unlike `captions::styles`/`render::presets` — since an intro/logo/music file is inherently a specific user's own asset (§17 frames this as the user registering their own files).
- [x] Template schema enhancements (§3/§20): add `intro`/`outro`/`watermark`/`background_music` fields (each an `Option<AssetReference>` — asset id + a few per-use overrides like watermark position/music volume), and template **versioning** (`version: u32`, a version history so an existing job's `template_id`+`template_version` stays pinned even if the template is later edited — §20's explicit requirement: "Không để user sửa template rồi làm thay đổi các job cũ").
  - `Template` gained 5 new fields, all `#[serde(default)]` (or `#[serde(default = "default_template_version")]` for `version`, defaulting to `1`) so every pre-existing built-in/custom template JSON still deserializes unchanged: `intro: Option<AssetReference>`, `outro: Option<AssetReference>`, `watermark: Option<WatermarkReference>`, `background_music: Option<BackgroundMusicReference>`, `version: u32`.
  - `AssetReference { asset_id }` is used bare for intro/outro (no per-use override needed). `WatermarkReference { asset_id, position: WatermarkPosition }` — `WatermarkPosition` is a new small enum (`TopLeft`/`TopRight`/`BottomLeft`/`BottomRight`/`Center`) rather than reusing `CaptionAnchor` (vertical-only) or `CaptionAlignment` (horizontal-only, for text) — neither already expresses a 2D corner, and no overlay-rendering engine exists yet to warrant a richer offset-based placement. `BackgroundMusicReference { asset_id, volume: f64 }` — linear gain, the same convention `AudioClipSettings::volume` already uses (§3's own YAML example literally writes `volume: 0.2`, matching linear gain, not a 0-100 percentage).
  - Asset-id references are validated against the real Asset Library at save/update time — `templates::validate_asset_references` (pure, unit-tested against a synthetic `HashSet`) checks every referenced id against `known_asset_ids`, which `commands::templates` reads fresh from `assets::io::list_assets` on every call. An unknown id is `TemplateError::UnknownAsset`, matching the existing `UnknownCaptionStyle`/`UnknownExportPreset` pattern exactly.
  - **Versioning/history mechanism**: every built-in and brand-new custom template starts at `version: 1`. A new command, `update_custom_template(template_id, project, input)`, is the only path that edits an existing custom template in place: it bumps `version` to `existing.version + 1`, and — before ever overwriting `<id>.json` — appends the *pre-update* full `Template` snapshot to a new `<id>.history.json` file (`templates::io::append_template_history`/`list_template_history`), a flat JSON array of every previous version, oldest first. Chosen over a `templates_dir/<id>/v<N>.json` subdirectory scheme for simplicity: one read/write per template rather than a directory listing plus N file reads. A new `get_template_version(template_id, version)` command resolves the exact content for a `template_id`+`version` pin: built-in catalog first (always exactly version 1), then the current on-disk custom template (if its version matches), then that template's history file. `update_custom_template` refuses to edit any of the 8 built-ins (`TemplateError::CannotEditBuiltIn`, mirroring `CannotDeleteBuiltIn`'s existing guard) — built-ins stay at `version: 1` forever and never grow a history file.
- [x] Multi-template batch (§11): given N videos and M templates, produce N×M outputs (`video01_tiktok.mp4`, `video01_youtube.mp4`, ...) — extends `batch::` to accept a list of template ids per batch rather than one, reusing the existing single-template pipeline per (video, template) pair, not a new render path. **Done 2026-09-06** (backend only — frontend is a later pass):
  - **API shape**: added a new sibling Tauri command, `start_multi_template_batch(media_paths, template_ids, config)` (`commands::batch`), rather than changing `start_batch`'s existing `BatchPipelineConfig::template_id: Option<String>` shape. `start_batch`'s own single-template call site/signature is completely untouched. The new command resolves every `template_ids` entry's real name (built-in-then-custom, reusing `batch::pipeline`'s own existing `resolve_template` lookup via a new narrow `pub(crate) resolve_template_name` sliver of it) **up front, before any job is created** — an unknown id fails the whole call immediately with `BatchError::UnknownTemplate`, never leaving some jobs pre-doomed deep into a batch.
  - **Job fan-out**: `BatchJobManager::create_multi_template_batch(media_paths, templates: Vec<(id, name)>, base_config)` (`batch::manager`) is a new, pure (no `AppHandle`), directly-unit-testable sibling of the existing `create_batch`, producing exactly N×M `BatchJob`s — one per `(video, template)` pair — reusing `create_batch`'s own `JobState`/`JobHandle` construction and `batch_order`/`job_batch` bookkeeping unchanged; only the enumeration loop differs. Each job's `name` is `"{filename} → {template_name}"` (e.g. `"video01.mp4 → TikTok"`) so the existing, unmodified Jobs UI still shows a sensible per-row name. `run_pipeline` itself was not forked or duplicated — every job still runs through the exact same `batch::pipeline::run_pipeline`.
  - **Output naming**: added one small additive field, `BatchPipelineConfig::output_suffix: Option<String>` (`None` = original `"edited"` default, unchanged for every existing single-template caller). `batch::pipeline::default_output_path` now takes this as an explicit `suffix: &str` parameter instead of hardcoding `"edited"`. A new `pub(crate) fn slugify_template_name(name: &str) -> String` (`batch::pipeline`) — lowercase, every non-ASCII-alphanumeric run collapsed to one `_`, leading/trailing `_` trimmed, `"template"` fallback for an all-punctuation name — is reused (not reinvented; no prior filename-slug helper existed in this codebase to reuse instead) to turn each template's real name into its slug (`"YouTube Shorts"` → `youtube_shorts`, `"Facebook Reel!! (v2)"` → `facebook_reel_v2`). Multi-template jobs get `output_suffix = Some(slug)`, producing exactly §11's `<video_stem>_<template_slug>.<ext>` convention (`video01_tiktok.mp4`).
  - **Concurrency**: unchanged and explicitly confirmed by test — a multi-template batch is still one batch, spawned via the same `spawn_batch_worker` (one dedicated thread, strictly sequential), just with more jobs enumerated up front. No parallel ffmpeg fan-out was introduced.
  - **Partial failure isolation**: confirmed by a real test (`one_failing_video_template_pair_in_a_multi_template_batch_does_not_abort_the_others`, `batch::manager`) — a nonexistent source path's 2 jobs fail with a real error while the other video's 2 jobs (placed *after* the failing ones in processing order) still reach `Completed` with real output files, mirroring this codebase's existing `media::import`/`broll::combine` per-item isolation precedent.
  - **Tests added**: `batch::pipeline` — `resolve_template_name` (built-in + unknown-id), `slugify_template_name` (simple/spaces/punctuation-runs/leading-trailing/unicode/all-punctuation-fallback/idempotence), `default_output_path` with a template-slug suffix. `batch::manager` — N×M fan-out pairing/naming, empty-template-list edge case, a real end-to-end 2 videos × 2 templates = 4 real synthesized-ffmpeg jobs producing 4 correctly-named real output files, and the partial-failure-isolation test above.
  - **Gap/scope note**: `start_multi_template_batch` does not itself validate `template_ids` non-empty (an empty list simply produces a batch with 0 jobs, same "no defensive count validation" posture `create_batch`'s own single-template path already has for `media_paths`); a repeated template id in `template_ids` is accepted, not rejected (produces 2 jobs per video for it — no correctness reason to police it).
  - **Real test-fixture bug found and fixed after this agent's own report** (caught by the concurrent Asset Library agent's own verification, then root-caused and fixed directly rather than re-delegated): the two real end-to-end tests initially used the same constant-sine-tone `synth_named_source` every other test in this file uses — but `tmpl_tiktok`/`tmpl_youtube_shorts` both carry real `silence_settings`, and a pure sine tone has none of real speech's spectral characteristics, so real Silero VAD correctly found **zero** speech segments across the whole clip, `vad::cutlist::build_cuts_from_speech_segments` proposed removing the entire media, and every job correctly failed with `RenderError::EmptyTimeline` — a real, correct pipeline outcome for that input, not a bug in the fan-out/naming logic itself. Fixed by changing `synth_named_source`'s own tone to an amplitude-modulated 220Hz signal (`sine=frequency=220:duration=3,tremolo=f=4:d=0.9`) — empirically verified against this project's real Silero model to produce one confident, whole-clip speech segment — rather than adding an `espeak-ng` dependency or marking the tests `#[ignore]`; every test in this file (including the two that need a real silence-removal outcome) now shares one real, VAD-detectable-by-default fixture with no new external tool dependency.
  - **Frontend compatibility fix**: `BatchPipelineConfig` gaining the new required `output_suffix` field broke `src/components/batch/StartBatchDialog.svelte`'s existing object-literal construction (Phase 11's own single-template Batch Jobs UI) — fixed by adding `output_suffix: null` there (a single-template batch started from that dialog has no per-job suffix to pick; the backend's own `"edited"` default naming applies, unchanged from before this phase).
- [x] Compile, run tests, fix errors, update docs, commit — both Phase U1 bullets (Asset Library/Template enhancements, Multi-template batch) done and merged together. `cargo fmt --check` clean, `cargo clippy --all-targets -- -D warnings` 0 warnings, `cargo test --lib` **841 passed, 0 failed, 4 ignored** (up from 805 pre-upgrade), `cargo test` (full workspace, including `src-tauri/tests/`) all green. `cargo run --bin export_bindings` succeeded. `pnpm run lint`/`check`/`build` all clean (229 files, 0 errors) after the `StartBatchDialog.svelte` fix above. Phase U2 (AI Auto Template + AI Template Generator) is next.

## Phase U2 — AI Auto Template + AI Template Generator

- [x] AI Auto Template (§7): given one video's real signals (duration, aspect, transcript, scene/highlight data — all already real from the original plan's Phase 10/11), ask the configured AI provider to recommend one of the catalog templates (built-in or custom), with a reason — never silently auto-applied, always a proposal the user accepts/changes. **Done 2026-09-06** (backend only — frontend Accept/Change Template/Customize/Run flow is a later pass):
  - **New module**: `crate::ai::auto_template` (`src-tauri/src/ai/auto_template.rs`) — same "propose, don't apply" shape every AI-consuming feature in this codebase already uses (`ai::edit_plan`/`ai::smart_edit`/`ai::media_tags`). `AiTemplateRecommendation { template_id, template_name, reason, confidence, suggested_aspect: Option<shorts::settings::ShortsAspect> }` — closed, strictly-typed, specta-typed. Reused `ShortsAspect` (Phase 11) rather than inventing a second aspect-ratio enum, since a recommendation may suggest an aspect different from the chosen template's own default `canvas` (e.g. Football Highlight defaults to 16:9 but is "also usable at 9:16").
  - **Real signals, all sourced from existing machinery, none re-derived a second way**: duration/aspect from `media::probe::probe` (Phase 3's real ffprobe wrapper); scenes from `media::scene::detect_scene_changes` (Phase 10/11's real ffmpeg scene-cut detector); important segments from `commands::highlights::run_detection` (Phase 10's real `Highlight` detection, reused directly — not re-scored); speech via a caller-supplied `Vec<TranscriptEntry>`, the same "caller passes the transcript in directly" convention `commands::highlights`/`commands::shorts` already use (this command never calls `transcribe_media` itself). "Content Type" (§7's own "Football Highlight" example) has no dedicated detector — left to the AI's own judgment, grounded in the transcript/highlights/scene signals already in the prompt, rather than building a separate labeled-taxonomy classifier (out of scope per §7's own intent — it asks for an informed recommendation, not a new classification feature).
  - **Prompt construction**: `build_auto_template_prompt`/`build_auto_template_request` (pure, unit-tested) — includes the probed media's duration/resolution/aspect/fps/audio-video presence, a transcript excerpt (entry count + total spoken duration + a ~2000-char excerpt, not the full transcript verbatim — a summary, per this pass's own brief), the scene-cut count/first/last timestamps, the top 5 highlights (time range/score/title/reason), and the **full real template catalog** (id/name/description/canvas WxH+ratio) the model must choose `template_id` from — plus explicit JSON schema/format instructions in the prompt text itself (same discipline `ai::smart_edit` already established, never hoping the model infers the shape).
  - **`parse_and_validate(raw, known_templates: &[Template])`**: rejects malformed JSON, an unsupported schema version, out-of-range confidence, and — the one validation in this crate's whole AI-validation surface that needs a real catalog rather than being bounded by the schema alone — an unknown `template_id` (checked against `known_templates`, which the caller loads fresh from `templates::all_templates()` + `templates::io::list_custom_templates` and passes in, keeping the validator itself pure and directly unit-testable against a synthetic in-test catalog fixture). `template_name` on the returned recommendation always comes from the resolved catalog entry's own real name — never trusted from the model's own response text — so `template_id`/`template_name` can never disagree.
  - **Command**: `suggest_template_for_media(app, media_path, transcript, ai_settings) -> AiTemplateRecommendation` (`commands/auto_template.rs`, registered in `lib.rs`) — orchestrates probe + scene detection + local-signal-only highlight detection (via `commands::highlights::run_detection` with `ai_settings: None`, deliberately: doing the highlight pass's own optional AI-semantic blend too would mean two AI calls per suggestion for no ask in §7) + one real `AIProvider::complete` call via the existing `commands::ai::resolve_api_key`/`build_provider` helpers (the same reuse pattern `commands::highlights::run_detection` already established) + `parse_and_validate`. Never applies the recommendation to anything itself — Phase 11's existing template-apply flow (or a small later wiring command, if genuinely missing) is the separate, explicit "apply this template" step.
  - **Tests**: prompt-construction content tests (confirm real duration/aspect/scene-count/highlight-titles/full-catalog all appear in the built prompt, plus an empty-signals edge case); every `parse_and_validate` rejection case (malformed JSON, unsupported version, out-of-range/negative confidence, unknown template id against both a real and a synthetic in-test catalog, an invalid `suggested_aspect` tag) plus the success cases (including that `template_name` is always catalog-derived, never the raw response's); a real HTTP round-trip against the existing `ai::test_http` mock server (reused, not reinvented) with a well-formed fixture response resolving to a real template id, plus malformed-response and unknown-template-id error-surfacing tests, all against a real synthesized ffmpeg clip (real probe/scene/VAD signals, not mocked). **No real AI API call was attempted anywhere in this pass** — no API key exists in this environment; every test either calls pure functions or talks to the local mock HTTP server.
  - **Verification**: `cargo fmt --check`/`cargo clippy --all-targets -- -D warnings` clean (0 warnings) for this pass's own files at the time of this writeup; `cargo test --lib` passed with this pass's own 23 new tests (7 in `ai::auto_template` prompt-construction, 12 in its `parse_and_validate`, 1 in `ai::error`'s stable-code test, 3 in `commands::auto_template`) on top of the pre-existing 841; `cargo run --bin export_bindings` succeeded (`suggestTemplateForMedia`/`AiTemplateRecommendation` present in `src/types/bindings.ts`); `pnpm run build` (non-WSL) succeeded. Ran concurrently with the AI Template Generator agent per this file's own instructions — both landed additively in shared files (`ai/mod.rs`, `ai/error.rs`, `commands/mod.rs`, `lib.rs`) with no conflicts; see that bullet for its own count/writeup.
- [x] AI Template Generator (§8): given a natural-language prompt, ask AI to produce a structured, schema-validated Template definition (closed schema, same "AI proposes → validate → preview → save" discipline as `ai::edit_plan`/`ai::smart_edit`) — never a raw/uncontrolled template. **Done 2026-09-06** (backend only — frontend Preview/Save Template UI is a later pass):
  - **New module**: `crate::ai::template_generator` (`src-tauri/src/ai/template_generator.rs`) — same "AI proposes → schema validation → never auto-applied" discipline as `ai::edit_plan`/`ai::smart_edit`/`ai::nl_command`.
  - **Option A chosen (documented in the module's own doc comment) over Option B**: the AI never invents a brand-new nested `CaptionStyle`/`RenderSettings`-level object. It picks **existing real catalog entries by id** — one of `captions::styles::all_caption_templates()`'s 6 built-in caption styles, one of `render::presets::all_presets()`'s real export presets, and (optionally) an already-registered `Asset` — plus a closed set of scalar/enum choices this codebase already has real types for: `zoom::ZoomIntensity`, `vad::CutParams` (three `i64` microsecond fields), `templates::TransitionSettings` (closed `TransitionType` + one duration), a new closed `GeneratedCanvasAspect` enum (a strict subset of `project::CanvasRatioPreset` excluding `Custom`, so no AI-invented width/height pair is ever trusted), `templates::{AssetReference, WatermarkReference, BackgroundMusicReference}`, and `templates::SportsOverlaySettings`. `GeneratedTemplateSpec` bundles exactly these fields — every one either a closed enum, a range-validated scalar, or a `String` id resolved against a real catalog. Option B (letting the AI also invent brand-new nested values, e.g. a custom caption style) was rejected: it would need its own tight per-field bounds validation duplicating `captions::styles`/`render::presets`'s own judgment calls, for no clear benefit over "pick the closest existing built-in, then tweak after Preview" — the same way a human already uses `templates::save_as_template_from_project`.
  - **Prompt construction**: `build_generate_template_prompt(nl_prompt, caption_styles, export_presets, assets)` (pure, unit-tested) — embeds the exact `GeneratedTemplateSpec` JSON schema/field list/enum values as explicit format instructions (same discipline `ai::nl_command`/`ai::smart_edit` already use), the user's verbatim natural-language request, and the real, current catalogs of caption styles/export presets/registered assets by id+name so the model references real ids instead of guessing at ones that don't exist.
  - **`parse_and_validate(raw, known_asset_ids)`**: rejects malformed JSON, an unsupported schema version, an empty `name`/`description`, any out-of-range numeric field (every `_us` duration bounded to `0..=5_000_000`, a `cross_fade` transition's duration additionally `> 0` and a `cut`'s exactly `0`, `duck_level` within `0.0..=1.0`, `background_music.volume` within `0.0..=4.0`, `system_prompt_prefix` capped at 2,000 characters), an unknown `caption_style_id`/`export_preset_id` (looked up directly against the real catalogs), and an unknown `intro`/`outro`/`watermark`/`background_music` asset id — the last reusing the *exact same* `templates::validate_asset_references` function `commands::templates::save_as_template`/`update_custom_template` already call (made `pub(crate)` for this reuse), not a reinvented parallel check. On success, returns a real `Template` (`is_built_in: false`, a fresh `custom_<uuid>` id, `version: 1`).
  - **Command**: `generate_template_from_prompt(app, nl_prompt, ai_settings) -> Template` (`commands/ai.rs`, registered in `lib.rs`) — thin `AppHandle`-resolving wrapper (loads the real caption-style/export-preset/asset catalogs) delegating to a plain, `AppHandle`-free `pub(crate) fn run_generation(...)` (same "Tauri command is a one-line resolve + delegate" split `commands::auto_template::run_suggestion` already established, chosen independently but landing on the same pattern) which builds the prompt, calls the configured provider, and validates the response. Returns the validated `Template` — does **not** save it to disk itself.
  - **Save-persistence gap — closed the same day**: this pass's own agent correctly identified that `commands::templates::save_as_template` is not a clean fit for persisting an already-fully-built generated `Template` (see its own reasoning: `SaveAsTemplateInput` demands a real `ProjectV1` to re-derive a *new* `Template` from, which doesn't exist here) and left it as a documented gap rather than forcing something awkward. Closed immediately after, exactly as suggested: a new `commands::templates::save_generated_template(app, template: Template) -> Template` — refuses to ever save over a built-in id (`TemplateError::CannotEditBuiltIn`, the same guard `update_custom_template` already uses), re-validates asset references fresh against the real Asset Library (`templates::validate_asset_references` — assets could have been removed between Generate and this explicit Save click), then calls the same real `templates::io::save_custom_template` primitive `save_as_template` itself already uses. Registered in `lib.rs`; verified via a full `cargo fmt`/`clippy`/`test --lib` (896 passed, unchanged) + `export_bindings` + `pnpm run build` pass after adding it.
  - **Tests**: prompt-construction content tests (confirm the schema instructions, the verbatim NL prompt, and every real caption-style/export-preset/asset id appear in the built prompt, plus an empty-catalog edge case); every `parse_and_validate` rejection case (malformed JSON, unsupported version, empty name, unknown caption style/export preset/asset id, every numeric out-of-range case listed above, an unknown enum tag smuggled into `transition_type` failing to deserialize rather than being interpreted) plus the success cases (a well-formed spec producing a real valid `Template`, one referencing real registered assets, a `sports_overlay` round-tripping with real `AudioRole`/`DuckingSettings` values); a real HTTP round-trip against the existing `ai::test_http` mock server (reused, not reinvented) with a well-formed fixture response, plus malformed-response/unknown-caption-style/unreachable-endpoint error-surfacing tests against `commands::ai::run_generation`. **No real AI API call was attempted anywhere in this pass** — no API key exists in this environment; every test either calls pure functions or talks to the local mock HTTP server.
  - **Verification**: `cargo fmt --check` clean; `cargo clippy --all-targets -- -D warnings` clean (0 warnings); `cargo test --lib` **896 passed, 0 failed, 4 ignored** (up from the pre-Phase-U2 841 — this pass's own 32 new tests: 27 in `ai::template_generator`, 1 in `ai::error`'s stable-code test, 4 in `commands::ai`'s `run_generation` tests — plus the concurrently-landed AI Auto Template agent's own 23, see that bullet); `cargo run --bin export_bindings` succeeded (`generateTemplateFromPrompt` present in `src/types/bindings.ts`); `pnpm run build` (non-WSL) succeeded. Ran concurrently with the AI Auto Template agent per this file's own instructions — both landed additively in shared files (`ai/mod.rs`, `ai/error.rs`, `commands/mod.rs`, `lib.rs`) with no conflicts; `templates::mod.rs`'s `validate_asset_references` was made `pub(crate)` (a one-keyword additive change) specifically for this pass's reuse.
- [x] Compile, run tests, fix errors, update docs, commit — both Phase U2 bullets (AI Auto Template, AI Template Generator) done and merged together, plus the `save_generated_template` gap closed same-day. `cargo fmt --check` clean, `cargo clippy --all-targets -- -D warnings` 0 warnings, `cargo test --lib` **896 passed, 0 failed, 4 ignored** (up from 841 pre-Phase-U2). `cargo run --bin export_bindings` succeeded. `pnpm run check`/`build` both clean (229 files, 0 errors). Phase U3 (History, Preview/Dry Run, Asset Library UI) is next.

## Phase U3 — History, Preview/Dry Run, Asset Library UI

- [x] Video Processing History (§21): persist every batch job's real record (input/output paths, template+version, AI prompt/result if any, timings, status, error, retry count) — a new local table, queryable, with a real UI (list/view/re-run/clone-settings). **Done 2026-09-06** (backend only — frontend History list/view/re-run/clone-settings UI is a later pass, per this pass's own instruction not to touch `src/`):
  - **New module**: `crate::history` (`src-tauri/src/history/{mod,io,error}.rs`). `HistoryEntry { id, batch_id, job_name, input_path, output_path, template_id, template_version, ai_prompt, ai_result, execution_plan, capcut_draft_path, started_at, ended_at, duration_us, status: BatchJobStatus, error, retry_count }` — specta-typed, reuses `BatchJobStatus`/`BatchPipelineConfig` directly rather than duplicating them.
  - **Storage-location decision**: the `history` table lives in the *same* SQLite file/connection `db::MediaLibrary` already manages (`media_library.sqlite3`), behind that *same* `Mutex<Connection>` Tauri state — **not** a second connection or a fourth persistence mechanism. `db::MediaLibrary`'s own doc comment already argues for one mutex over one file for exactly this kind of small, local, single-writer-per-command data; two locks over one file would buy nothing here. `history::io::init_schema` is called once at startup (`lib.rs`'s `run()` setup, right after `commands::media::init_media_library` opens the connection), against that same connection. The Tauri state type keeps the name `MediaLibrary` (predates this table) — not renamed, to avoid touching every existing `commands::media::*` call site for a cosmetic reason; both `history` and `commands::history`'s own doc comments call this out explicitly.
  - **Which §21 fields are real vs. structural-only**: real and populated — `input_path`, `output_path`, `template_id`, `started_at`/`ended_at`/`duration_us`, `status`, `error`, `retry_count`. `template_version` is real (resolved fresh via a new `batch::pipeline::resolve_template_version`, a sibling of the existing `resolve_template_name`) but with one honestly-documented narrow race: it's resolved at history-*write* time (right after the job finishes), not re-threaded out of `run_pipeline`'s own earlier template resolution at job-*start* — widening `run_pipeline`'s return type to carry that value out would be a more invasive change than this pass's scope (several existing tests destructure its `Ok` as a bare `PathBuf`), so a custom template edited *while* one of its own jobs is still mid-render could show a slightly-stale version; built-ins never hit this (always version 1). `execution_plan: BatchPipelineConfig` is the real, already-serializable stand-in for **both** §21's "Editing plan" and "Execution plan" rows — this pipeline has no separate AI-authored `EditPlan`-shaped object per batch job, so a second bespoke plan type would have nothing real to represent; stored as a JSON TEXT column. `ai_prompt`/`ai_result` are structurally present (closed `Option<String>` columns) but always `None` — checked before writing this module: `batch::pipeline::run_pipeline` does not invoke any AI step (`ai::smart_edit`/`ai::edit_plan`/`ai::auto_template`) anywhere in its own pipeline today. `capcut_draft_path: Option<String>` is the one honest rename of §21's "CapCut worker" field — this app's CapCut integration is direct draft-file export, not a live worker process (this file's own audit note), so there's no worker id to record; always `None` today since no batch job pipeline calls `capcut::export` yet either.
  - **Retry / re-run semantics**: a `HistoryEntry.id` is exactly the originating `BatchJob`'s own `job_id` — not a separate uuid. `history::io::record_terminal` is a single SQL upsert (`INSERT ... ON CONFLICT(id) DO UPDATE SET ..., retry_count = history.retry_count + 1`, computed atomically inside SQLite, no separate read-then-write race) — the first time a job reaches a terminal state it inserts a fresh row (`retry_count: 0`); every subsequent time the *same* `job_id` reaches a terminal state again (i.e. after `batch::manager::prepare_retry`'s own documented "same job_id, reset to Queued" retry semantics), the same row is updated in place and `retry_count` increments — never a second, disconnected row per retry. A **re-run** (§21's separate "Re-run" action) is deliberately different: `history::build_rerun_config`/`build_rerun_with_template_config` (pure, unit-tested) hand a fresh `BatchPipelineConfig` to `BatchJobManager::create_batch` (Phase 11's real batch creation, reused unchanged, plus `spawn_batch_worker`) as a **brand-new** batch/job id — the old history entry is never touched again, and the new job earns its own separate row once it finishes.
  - **Wiring into `batch::manager`**: `run_job_with_events` (the real, `AppHandle`-dependent worker-thread entry point) calls a new `build_history_entry` (pure, no `AppHandle`) to build the row from a just-finished `JobHandle`, then `record_history_for_job` locks the shared `MediaLibrary` connection only briefly — just for this one write, never held across `process_job`'s own potentially-long pipeline run — and calls `history::io::record_terminal`. Wired into **both** paths that can end a job in a terminal state: the normal post-`process_job` path, and the early-failure branch where `resolve_pipeline_paths` itself failed (e.g. ffmpeg not resolvable) before any real stage ran. A real database write failure here is logged (`tracing::warn!`) and never propagated — recording history must never be *why* an otherwise-successful job appears to have failed. Confirmed directly (per this task's own instruction to check, not assume) that `BatchJobManager`'s prior state really was purely in-memory (`HashMap<String, JobHandle>`, nothing persisted anywhere) — this pass is what gives it durability across app restarts.
  - **Commands** (`src-tauri/src/commands/history.rs`): `list_history(limit?, offset?)` (real `LIMIT`/`OFFSET` pagination, newest-`started_at`-first, same query-building convention `db::search_media` established), `get_history_entry(id)`, `clone_history_entry_settings(id) -> BatchPipelineConfig` (§21 "Clone settings" — returns the exact config, starts nothing itself), `rerun_from_history(id) -> RerunResult { batch_id, job_ids }` (§21 "Re-run"), `rerun_from_history_with_template(id, new_template_id) -> RerunResult` (§21 "Run with another template" — swaps only `template_id`, does not pre-validate it against the real catalog, matching `create_batch`/`start_batch`'s own existing no-upfront-validation posture for a single-template batch — only `start_multi_template_batch`'s N×M fan-out validates up front, since only it has a large blast radius to protect). `delete_history_entry(id)` added too (not one of §21's own listed actions, but a low-cost real primitive for a future "clear this run" row action). `RerunResult` is a real named specta-typed struct, not a bare tuple, so field names survive the Tauri IPC boundary into TypeScript.
  - **"Download output"/"View logs" — no new backend surface**: §21 lists these as allowed actions, but both map onto mechanisms this codebase already has for real — "Download output" is a frontend "reveal in folder" action against the real `output_path` already on the entry; "View logs" is Phase 12's existing `get_logs_folder_path`/`open_logs_folder`. Documented in `commands::history`'s own module doc comment; no duplicate command added.
  - **Tests added (24)**: `history::io` (11) — real SQLite round trip (in-memory `Connection`, mirroring `db::open_in_memory`'s own precedent), retry-same-row-not-a-duplicate (including a second retry bumping the count again), a different job id creating a genuinely new row, newest-first ordering, real `LIMIT`/`OFFSET` pagination across 5 rows, delete, Failed/Cancelled status round-tripping, `None`-across-the-board round-tripping (template/ai/capcut fields), and every one of the 9 `BatchJobStatus` variants round-tripping through the TEXT column mapping. `history::error` (1) — stable error-code mapping. `history::mod` (3) — `build_rerun_config` returns the entry's own plan unchanged, `build_rerun_with_template_config` swaps only `template_id` (every other field asserted unchanged), and doesn't mutate the original entry. `batch::pipeline` (3) — `resolve_template_version` for a built-in (always 1), an unknown id (errors), and a real saved custom template's real version (3). `batch::manager` (6) — `build_history_entry` returns `None` for a non-terminal job; a real end-to-end `process_job` run's entry round-trips through `record_terminal` with every field asserted (including `execution_plan == handle.config` exactly and `ai_prompt`/`ai_result`/`capcut_draft_path` all `None`); template-version resolution both with and without a `templates_dir` (the early-failure case); a real retried-failed-job's second terminal recording bumps `retry_count` to 1 on the *same* row (not a duplicate); and a full real end-to-end proof that a history-backed re-run and "run with another template" both actually complete via real `create_batch` + `process_job` against a real synthesized clip (reusing this file's own `synth_named_source` tremolo-modulated fixture for templates with real `silence_settings`).
  - **Verification**: `cargo fmt --check` clean; `cargo clippy --all-targets -- -D warnings` 0 warnings; `cargo test --lib` **932 passed, 0 failed, 4 ignored** (up from 896 pre-Phase-U3; this pass's own delta is the 24 tests listed above — the remaining +12 are the concurrently-landed Preview/Dry-Run agent's own `batch::dry_run` tests, see that bullet for its own writeup); `cargo run --bin export_bindings` succeeded (`listHistory`/`getHistoryEntry`/`cloneHistoryEntrySettings`/`rerunFromHistory`/`rerunFromHistoryWithTemplate`/`deleteHistoryEntry`/`HistoryEntry`/`RerunResult` all present in `src/types/bindings.ts`); `pnpm run build` (non-WSL) succeeded. Ran concurrently with the Preview/Dry-Run agent per this file's own instructions — both landed additively in shared files (`lib.rs`, `batch/manager.rs`, `batch/pipeline.rs`, `commands/mod.rs`) with no conflicts (re-read-and-retry was needed a few times as that agent's own edits landed first).
- [x] Preview / Dry Run (§18): validate + show the real would-be execution plan (which template/asset/AI decision would apply) for one video without actually rendering, before committing a large batch. **Done 2026-09-06** (backend only — frontend "Test 1 Video" / "Apply to 100" UI is a later pass):
  - **New module**: `crate::batch::dry_run` (`src-tauri/src/batch/dry_run.rs`) — a genuinely separate, read-only function from `run_pipeline`, not a `dry_run: bool` flag threaded through the real pipeline (a shared boolean-gated path risks a dry-run branch silently skipping a real safety check, or vice versa). Instead it calls into the *exact same* shared sub-pieces `run_pipeline` itself uses — `batch::pipeline::resolve_template`/`default_output_path`/`stage_failed` (all promoted from private to `pub(crate)` for this reuse, unchanged otherwise) plus the real `media::probe`/`vad` cores — so "what a dry run predicts" and "what a real job actually does" can never silently drift apart from duplicated logic.
  - **`DryRunResult`** (specta-typed, closed schema): `{ input: DryRunInput, resolved_template: Option<Template>, ai_decision: Option<AiTemplateRecommendation>, editing_plan: DryRunEditingPlan, expected_output: DryRunExpectedOutput }` — §18's exact field list minus one, see below. `DryRunInput` is the real probed `path`/`duration_us`/`width`/`height`/`has_audio`/`has_video`. `DryRunEditingPlan` is `{ silence_removal: SilenceRemovalPlan, captions: CaptionsPlan }` — `SilenceRemovalPlan` reports `enabled`/`params`/`source` (`Explicit` vs `Template`, mirroring `run_pipeline`'s own precedence exactly) plus a real, VAD-computed `predicted_removed_us`; `CaptionsPlan` reports `enabled`/`reason`/the real resolved `transcription_model_id`, never executing anything. `DryRunExpectedOutput` is the real `output_path` (`pipeline::default_output_path`'s own naming logic, unchanged), a real `predicted_duration_us` (probed duration minus the VAD-computed removal, only when computed), and the resolved export preset's real `width`/`height`/`container`/`video_codec`.
  - **Which analysis steps run for real vs. are only described (documented in the module's own doc comment, per this pass's task brief)**: **probing** (`media::probe::probe`) always runs for real. **VAD scoring/cutlist** (`vad::SileroVadProvider::score_chunks` + `segments_from_scores` + `build_cuts_from_speech_segments`) runs for real, but only when the resolved config would actually enable silence removal (explicit `remove_silence`, or a resolved template's own `silence_settings` — run_pipeline's exact fallback) *and* the source has a real audio track — real local-model inference, no network/GPU dependency, the same cost `run_pipeline`'s own Editing stage always pays, verified in this pass's own tests to run in well under a second against a 3s synthesized clip. This is the one step that makes `predicted_duration_us` a real computed number. **Scene-change detection** is deliberately *not* called directly by a dry run — checked first: `run_pipeline` itself never calls `media::scene::detect_scene_changes` for its own editing decisions either (`BatchPipelineConfig::template_id`'s own doc comment: zoom/transition/sports-overlay template settings aren't applied by this pipeline), so there is no real batch-job decision here for scene detection to inform; it still runs indirectly whenever the AI Auto Template path below is taken, since that already-real feature's own signal-gathering includes it. **Transcription** (real Whisper inference) is never actually run — a multi-second-to-minutes model inference is not something a preview should pay for; `CaptionsPlan` reports what real config *would* run (and doesn't even check whether the model is installed — unlike `run_pipeline`, which errors on an uninstalled model, since describing the plan doesn't need the model present on disk).
  - **AI Decision wiring**: `ai_decision: Option<AiTemplateRecommendation>` reuses the real Phase U2 `ai::auto_template::AiTemplateRecommendation` type verbatim — never a new one. Populated only when `config.template_id` is `None` (no template already chosen) **and** real `ai_settings` were provided (the same "AI is optional, never required" discipline every AI-consuming feature in this codebase follows) — when both hold, `run_dry_run_with_ai` calls `commands::auto_template::run_suggestion` verbatim (the real, already-shipped Phase U2 feature, not a second parallel "what would the AI decide" implementation). If a template was already chosen, or no AI settings were given, `ai_decision` stays honestly `None` — never fabricated — and a populated `ai_decision` never overwrites `resolved_template` itself (shown alongside it as a proposal only, per that feature's own "propose, don't apply" discipline).
  - **Honest scope note**: §18's own field list includes a "CapCut Execution Plan" — this codebase's confirmed architectural decision (this file's own "Explicitly out of scope" section) is direct draft-file export, not CapCut GUI/RPA automation, and `run_pipeline` itself never builds a CapCut export at all (it renders straight through `render::build_render_graph`/ffmpeg) — there is no real "CapCut execution" step for a dry run to describe. `DryRunExpectedOutput`'s own resolution/container/codec fields are this codebase's honest equivalent: the real resolved *render* plan a batch job would execute.
  - **Command**: `dry_run_batch_job(app, media_path, config, ai_settings: Option<AiProviderSettings>) -> Result<DryRunResult, AppErrorPayload>` (`commands/batch.rs`, registered in `lib.rs`) — a one-line delegate to `batch::dry_run::run_dry_run_for_media`, matching `commands::batch`'s own "thin wrapper, real logic lives in `batch::`" convention exactly (same split `start_multi_template_batch` already established). `run_dry_run_for_media` (AppHandle-dependent: resolves ffmpeg/ffprobe/templates paths + the real built-in+custom template catalog) delegates to `run_dry_run_with_ai` (Tauri-free, AI-aware) which delegates to `run_dry_run` (Tauri-free, AI-free core) — the same three-layer "AppHandle-resolving wrapper -> AppHandle-free real logic" split `commands::auto_template::suggest_template_for_media`/`run_suggestion` already established, chosen so every non-AI test case needs no mock AI server and the AI-aware tests need no running Tauri app.
  - **Tests**: 12 new tests in `batch::dry_run`, all against real ffmpeg-synthesized clips (no template configured; a real built-in template — asserting a real VAD-computed `predicted_removed_us`/`predicted_duration_us` and the template's own real export preset fallback; an explicit `remove_silence` override winning over the template's own; a real-silence fixture predicting ~the whole duration removed, succeeding where a real render would fail with `EmptyTimeline`, proving a dry run never actually renders; captions configured with an *uninstalled* transcription model succeeding, proving transcription/install-checking never actually runs; the same up-front `TranscriptionModelRequired`/`UnknownTemplate`/`MediaNotFound` validation as `run_pipeline`; an explicit assertion that no output file — nor even a non-empty `batch_output` directory — exists after a fully-configured dry run; and three AI-decision tests against the existing `ai::test_http` mock server, confirming the recommendation is surfaced without ever overwriting `resolved_template`, is skipped when a template was already chosen (even against a deliberately unreachable AI endpoint, proving no call is attempted), and is skipped when no AI settings are given at all).
  - **Verification**: `cargo fmt --check`/`cargo clippy --all-targets -- -D warnings` clean (0 warnings). `cargo test --lib` **932 passed, 0 failed, 4 ignored** (up from the pre-Phase-U3 896 — this pass's own 12 new tests in `batch::dry_run`, plus the concurrently-landed Video Processing History agent's own tests, see that bullet for its own count/writeup once it lands). `cargo run --bin export_bindings` succeeded (`dryRunBatchJob`/`DryRunResult`/`DryRunInput`/`DryRunEditingPlan`/`SilenceRemovalPlan`/`CaptionsPlan`/`DryRunExpectedOutput`/`SilenceSettingsSource` all present in `src/types/bindings.ts`). `pnpm run build` (non-WSL) succeeded (243 modules, 0 errors). Ran concurrently with the Video Processing History agent per this file's own instructions — both touched shared files (`batch/pipeline.rs`, `batch/manager.rs`, `lib.rs`) additively with no real conflicts (the History agent's own `resolve_template_version` and this pass's `resolve_template`/`default_output_path`/`stage_failed` visibility bumps landed side by side in the same file).
- [ ] Asset Library UI + Template versioning UI (frontend for Phase U1's backend, if not already built alongside it).
- [~] Compile, run tests, fix errors, update docs, commit — **History and Preview/Dry Run (backend) done and merged; Asset Library UI/Template versioning UI still pending, so Phase U3 as a whole isn't closed out yet.** Combined re-verification after both backend passes landed: `cargo fmt --check` clean, `cargo clippy --all-targets -- -D warnings` 0 warnings, `cargo test --lib` **932 passed, 0 failed, 4 ignored** (up from 896 pre-Phase-U3). `cargo run --bin export_bindings` succeeded. `pnpm run check`/`build` both clean (229 files, 0 errors).

## Phase U4 — Smart Automation (rules / watch-folder)

- [ ] A minimal rule engine (§27): `WHEN <trigger> IF <condition> THEN <action-sequence>`, starting with a real filesystem watch-folder trigger (a new dependency — a Rust folder-watcher crate — since nothing in this codebase currently watches the filesystem for changes) plus reuse of the existing AI-analyze → template-apply → batch-export pipeline as the action sequence. Explicitly scoped small per the spec's own "không over-engineer nếu codebase hiện tại chưa cần" instruction — one trigger type (new file in folder), not a general-purpose scheduler.
- [ ] Compile, run tests, fix errors, update docs, commit.

---

## Explicitly out of scope / deferred, by the user's own decision

- **CapCut GUI/RPA automation, CapCut Worker Pool, CapCut-process crash recovery** (§12/§15/§16 as literally written) — the user chose to keep direct draft-file export instead (2026-09-06). Revisit only if the user explicitly asks later.
- **Multi-machine worker pool / distributed scheduling** (§15's "Machine"/"Worker 01/02/03" concept) — this is a single-machine desktop app; nothing in this upgrade introduces distributed workers.
- **Visual (drag-and-drop) Template Builder UI** (§4) — the *data model* (a structured step-list JSON) may fall out naturally from Phase U1/U2's work, but the drag-and-drop UI itself is a large, separate frontend undertaking not yet scheduled into a phase above; revisit after U1–U4 land if still wanted.
- **AI cost/token tracking, AI response caching by video-hash** (§22) — not yet scheduled into a phase; flagged as a real gap, not forgotten.
- **Randomization with seed** (§28) — not yet scheduled into a phase.
